# flags-2-env-flutter

Connection snapshots use a sealed `ConnectionStatus` hierarchy rather than a
Boolean. `StatusCard` renders it with a default-free switch expression, so a
new state is a compile-time error until every UI branch is handled. The widget
suite exercises every currently declared variant.

Flutter for mobile, desktop, and mobile web. No React. UI lives in `lib/src/`.
